"""Atlas decomposition engine (deductive v1).

The moat is the decomposition, not the website. This package is the engine:
ingest -> classify -> extract -> verify -> assemble, producing a typed,
provenance-backed knowledge graph for deductive (math/theorem) books.
"""

__version__ = "0.1.0"
